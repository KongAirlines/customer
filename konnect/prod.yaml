_defaults:
  kongctl:
    namespace: customer-information-prod

control_planes:
  - ref: kongairlines-prod
    _external:
      selector:
        matchFields:
          name: kongairlines-prod

apis:
  - ref: customer-information-prod-api
    name: KongAir Customer Information
    description: Production Catalog API owned by the Customer service team.
    versions:
      - ref: customer-information-prod-0-1-0
        version: !file ../openapi/versions/0.1.0.yaml#info.version
        spec: !file ../openapi/versions/0.1.0.yaml
    publications:
      - ref: customer-information-prod-publication
        portal_id: !lookup {name: KongAirlines Production}
        visibility: public
        auth_strategy_ids:
          - !lookup {name: kongairlines-key-auth-prod}
    implementations:
      - ref: customer-information-prod-control-plane
        type: control_plane
        control_plane:
          control_plane_id: !ref kongairlines-prod#id
